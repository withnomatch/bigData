# 特征提取优化实验汇报

---

## 第1页：研究背景与实验设计

### 研究目标

针对StackOverflow Oracle Database 152,758条问答数据，对比三种特征提取方案对K-Means聚类效果的影响，探索最优文本向量化策略。

### 三种特征提取方案

| 方案 | 核心思路 | 问题向量生成方式 |
|------|---------|----------------|
| **纯TF-IDF** | 基于词频-逆文档频率的稀疏表示 | TF-IDF稀疏向量 → L2归一化 |
| **纯Word2Vec** | 基于上下文语义的稠密表示 | 词向量简单平均 → L2归一化 |
| **TF-IDF加权Word2Vec** | 融合统计权重与语义信息 | 词向量TF-IDF加权平均 → L2归一化 |

### TF-IDF参数调整范围

| 参数 | 调整范围 | 默认值 | 作用 |
|------|---------|--------|------|
| 词汇表大小 (max_features) | 3,000 ~ 10,000 | 5,000 | 控制特征维度，平衡信息量与噪声 |
| 最小文档频率 (min_df) | 3 ~ 10 | 5 | 过滤低频词，减少噪声 |
| 最大文档频率 (max_df) | 0.8 ~ 0.95 | 1.0 | 过滤高频通用词，提升区分度 |

### 文本预处理优化策略

- **定制停用词列表**：去除Oracle领域通用词（oracle, sql, database等）及功能词
- **词形还原**：统一时态和单复数形式（tables→table, querying→query）
- **专业术语识别**：保留ORA-错误码、PL/SQL关键字、数据类型等专有词汇

### 评估指标

- **轮廓系数 (Silhouette)**：衡量聚类紧密度与分离度，[-1, 1]，越大越好
- **WCSS**：簇内平方和，越小越好
- **语义一致性 (Coherence)**：簇内平均余弦相似度，越大越好
- **聚类分布均衡性**：最大/最小簇大小、标准差

---

## 第2页：实现原理

### TF-IDF特征提取

$$\text{TF-IDF}(t,d) = \text{TF}(t,d) \times \log\frac{N}{\text{DF}(t)}$$

- **TF (词频)**：词在文档中出现的频率
- **IDF (逆文档频率)**：$\log(N/DF(t))$，降低常见词权重
- **Pipeline**：Tokenizer → CountVectorizer → IDF → Normalizer

### Word2Vec特征提取

- **Skip-gram模型**：给定中心词预测上下文词，学习词的分布式表示
- **问题向量**：对文档中所有词向量取简单平均
$$\vec{v}_{doc} = \frac{1}{|W|}\sum_{w \in W} \vec{v}_w$$
- **Pipeline**：Tokenizer → Word2Vec → Normalizer

### TF-IDF加权Word2Vec特征提取（创新方案）

- **核心思想**：用TF-IDF权重对词向量加权，使重要词对文档向量贡献更大
- **问题向量**：
$$\vec{v}_{doc} = \frac{\sum_{w \in W} \text{TF-IDF}(w,d) \cdot \vec{v}_w}{\sum_{w \in W} \text{TF-IDF}(w,d)}$$
- **实现流程**：
  1. 训练Word2Vec获取词向量 → 收集为广播变量
  2. 训练CountVectorizer + IDF获取TF-IDF权重 → 构建word→idf映射
  3. UDF对每个文档：计算加权平均 → L2归一化

### 参数对特征空间的影响

| 参数 | 增大效果 | 减小效果 |
|------|---------|---------|
| max_features | 保留更多低频专业词，维度增大 | 仅保留高频核心词，信息损失 |
| min_df | 过滤更多噪声词，词汇表更精炼 | 保留更多罕见词，可能引入噪声 |
| max_df | 保留更多通用词，区分度下降 | 过滤通用词，提升主题区分度 |

---

## 第3页：实验结果

### 三种方案对比（采样10%数据，K=50，vector_size=50）

| 指标 | TF-IDF | Word2Vec | TF-IDF+Word2Vec |
|------|--------|----------|-----------------|
| **轮廓系数** | 0.0189 | 0.1091 | **0.1376** |
| **WCSS** | 13976.82 | **4877.56** | 6470.52 |
| **语义一致性** | 0.4063 | **0.8082** | 0.7461 |
| **非空簇数** | 50/50 | 50/50 | 50/50 |
| **最大簇大小** | 5042 | 791 | **754** |
| **最小簇大小** | 2 | 51 | **123** |
| **簇大小标准差** | 717.4 | 151.4 | **130.4** |
| **运行时间** | 56.87s | 378.41s | 853.78s |

### TF-IDF参数调整对比实验

| 参数组合 | max_features | min_df | max_df | 轮廓系数 | WCSS | 语义一致性 |
|---------|-------------|--------|--------|---------|------|-----------|
| 基线 | 5000 | 5 | 1.0 | 0.0189 | 13976.82 | 0.4063 |
| 小词汇表 | 3000 | 5 | 1.0 | 0.0156 | 14231.45 | 0.3892 |
| 大词汇表 | 10000 | 5 | 1.0 | 0.0213 | 13654.28 | 0.4187 |
| 严格过滤 | 5000 | 10 | 1.0 | 0.0178 | 14102.33 | 0.3956 |
| 宽松过滤 | 5000 | 3 | 1.0 | 0.0201 | 13845.67 | 0.4123 |
| 过滤通用词 | 5000 | 5 | 0.8 | 0.0245 | 13567.91 | 0.4312 |
| 严格过滤+去通用 | 5000 | 10 | 0.9 | 0.0221 | 13789.45 | 0.4201 |
| **最优组合** | **10000** | **5** | **0.8** | **0.0289** | **13412.56** | **0.4456** |

### 预处理优化效果对比

| 预处理策略 | 轮廓系数变化 | 语义一致性变化 |
|-----------|------------|-------------|
| 基线（仅分词+去停用词） | 0.0189 | 0.4063 |
| +定制Oracle停用词 | +0.0034 | +0.0189 |
| +词形还原 | +0.0067 | +0.0234 |
| +专业术语识别 | +0.0045 | +0.0156 |
| **全部优化** | **+0.0156** | **+0.0612** |

---

## 第4页：结果分析

### 1. 三种方案对比分析

**轮廓系数**：TF-IDF+Word2Vec (0.1376) > Word2Vec (0.1091) > TF-IDF (0.0189)

- TF-IDF+Word2Vec相比纯Word2Vec提升26.3%，相比纯TF-IDF提升628%
- TF-IDF加权有效提升了聚类分离度

**聚类分布均衡性**：TF-IDF+Word2Vec最优

- TF-IDF存在严重"巨型簇"问题（最大簇5042，占总数据32.9%）
- Word2Vec和TF-IDF+Word2Vec最大簇仅791/754，分布更均匀
- TF-IDF+Word2Vec最小簇123，远大于TF-IDF的2和Word2Vec的51

**语义一致性**：Word2Vec (0.8082) > TF-IDF+Word2Vec (0.7461) > TF-IDF (0.4063)

- Word2Vec簇内语义最紧密，但关键词缺乏可解释性（如"want, without, also"）
- TF-IDF+Word2Vec兼顾语义一致性与关键词可解释性

### 2. TF-IDF参数调整分析

- **max_features**：从3000增至10000，轮廓系数提升36.5%。更大词汇表保留更多专业术语，但边际收益递减
- **min_df**：从3增至10，轮廓系数略降。过严过滤会丢失罕见专业词
- **max_df**：设为0.8后轮廓系数提升29.6%。过滤oracle/sql/database等高频通用词显著提升区分度

### 3. 预处理优化分析

- **定制停用词**效果最直接，去除领域通用词后主题更聚焦
- **词形还原**统一了tables/table等变体，降低了特征稀疏性
- **专业术语识别**保留了ORA-错误码等关键信息，提升了专业聚类质量

### 4. 聚类语义验证

| 方案 | 典型聚类主题 | 关键词可解释性 |
|------|------------|--------------|
| TF-IDF | 日期处理、PL/SQL、连接配置 | **强**（date, procedure, connect） |
| Word2Vec | 通用查询、安装问题 | 弱（want, without, also） |
| TF-IDF+Word2Vec | 日期处理、通用查询、安装问题 | **中强**（day, date, via, working） |

---

## 第5页：结论与展望

### 核心结论

1. **TF-IDF+Word2Vec是最优特征提取方案**：轮廓系数最高(0.1376)，聚类分布最均衡(标准差130.4)，兼顾语义质量与可解释性

2. **TF-IDF参数优化效果显著**：
   - max_df=0.8（过滤高频通用词）是提升最大的单一参数调整
   - 最优参数组合：max_features=10000, min_df=5, max_df=0.8

3. **预处理优化可进一步提升效果**：定制停用词+词形还原+专业术语识别联合使用，轮廓系数提升82.5%

4. **各方案适用场景不同**：
   - TF-IDF：需要关键词可解释性、快速原型验证
   - Word2Vec：追求语义一致性、不关注关键词
   - TF-IDF+Word2Vec：综合效果最优，推荐作为最终方案

### 局限性

- 采样10%数据实验，全量数据效果可能不同
- Word2Vec简单平均丢失词序信息
- 轮廓系数绝对值偏低(0.1376)，仍有优化空间

### 未来优化方向

- **引入BERT等预训练模型**：获取上下文相关的句子嵌入
- **尝试LDA主题模型**：结合主题分布与词向量
- **深度聚类方法**：如Deep Embedded Clustering，端到端优化
- **自动参数搜索**：网格搜索或贝叶斯优化寻找最优参数组合
