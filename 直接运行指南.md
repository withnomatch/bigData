## 部署好后直接运行指南
## 集群环境

| 节点 | IP地址 | 角色 |
|------|--------|------|
| node5 | 10.176.62.230 | Master (HDFS NameNode, Spark Master) |
| node1 | 10.176.62.231 | Worker (Spark Worker) |
| node2 | 10.176.62.232 | Worker (Spark Worker) |

**密码**: `Fdwangp123`

**软件版本**:
- Spark 2.0.0 (支持Python 2)
- Hadoop 2.7
- Python 2.7

---

## 快速开始（已部署环境）

如果环境已经部署好，按以下步骤运行：

### 1. 连接集群

```bash
ssh root@10.176.62.230
# 密码: Fdwangp123
```

### 2. 启动服务

```bash
# 启动HDFS
/opt/hadoop/sbin/start-dfs.sh

# 启动Spark Master
/opt/spark-2.0.0-bin-hadoop2.7/sbin/start-master.sh

# 启动Spark Workers
ssh root@10.176.62.231 "/opt/spark-2.0.0-bin-hadoop2.7/sbin/start-slave.sh spark://10.176.62.230:7077"
ssh root@10.176.62.232 "/opt/spark-2.0.0-bin-hadoop2.7/sbin/start-slave.sh spark://10.176.62.230:7077"

# 验证
jps
```

### 3. 运行任务

#### 3a. 上传脚本到集群

```bash
# 从本地上传D实验脚本到Master节点
scp d_word2vec_dedup_experiment.py root@10.176.62.230:/root/

# 如果需要，也上传到HDFS
# hdfs dfs -put d_word2vec_dedup_experiment.py /user/root/scripts/
```

#### 3b. 运行D实验（全部5组）

```bash
export SPARK_HOME=/opt/spark-2.0.0-bin-hadoop2.7
export HADOOP_HOME=/opt/hadoop
export PATH=$SPARK_HOME/bin:$SPARK_HOME/sbin:$HADOOP_HOME/bin:$HADOOP_HOME/sbin:$PATH

# 运行全部D1-D5实验
$SPARK_HOME/bin/spark-submit \
    --master spark://10.176.62.230:7077 \
    --driver-memory 2g \
    --executor-memory 4g \
    --executor-cores 2 \
    --num-executors 2 \
    --py-files /usr/lib64/python2.7/site-packages/numpy \
    /root/d_word2vec_dedup_experiment.py \
    --input hdfs://node5:9000/user/root/data/fixed_eval_sample_10pct_plus_300 \
    --output hdfs://node5:9000/user/root/output/d_word2vec_experiment \
    --pairs hdfs://node5:9000/user/root/data/dedup_pairs_300_reviewed.csv \
    --k 45 \
    --max-features 5000 \
    --min-df 5 \
    --max-df 0.9 \
    --vector-size 100 \
    --min-count 5 \
    --max-iter 30 \
    --similarity-threshold 0.55 \
    --dedup-top-per-cluster 80 \
    --title-weight 2.0 \
    --body-weight 1.0 \
    --experiments 1,2,3,4,5
```

#### 3c. 只运行部分实验

```bash
# 只运行D1(TF-IDF基线)和D4(TF-IDF加权Word2Vec)
$SPARK_HOME/bin/spark-submit \
    --master spark://10.176.62.230:7077 \
    --driver-memory 2g \
    --executor-memory 4g \
    --executor-cores 2 \
    --num-executors 2 \
    --py-files /usr/lib64/python2.7/site-packages/numpy \
    /root/d_word2vec_dedup_experiment.py \
    --input hdfs://node5:9000/user/root/data/fixed_eval_sample_10pct_plus_300 \
    --output hdfs://node5:9000/user/root/output/d_word2vec_experiment \
    --pairs hdfs://node5:9000/user/root/data/dedup_pairs_300_reviewed.csv \
    --k 45 \
    --experiments 1,4

# 只运行D2(Word2Vec平均向量)
$SPARK_HOME/bin/spark-submit \
    --master spark://10.176.62.230:7077 \
    --driver-memory 2g \
    --executor-memory 4g \
    --executor-cores 2 \
    --num-executors 2 \
    --py-files /usr/lib64/python2.7/site-packages/numpy \
    /root/d_word2vec_dedup_experiment.py \
    --input hdfs://node5:9000/user/root/data/fixed_eval_sample_10pct_plus_300 \
    --output hdfs://node5:9000/user/root/output/d_word2vec_experiment \
    --pairs hdfs://node5:9000/user/root/data/dedup_pairs_300_reviewed.csv \
    --k 45 \
    --experiments 2
```

#### 3d. 运行旧版基线（可选）

```bash
# 旧版基线实验（K=50, 10%采样）
$SPARK_HOME/bin/spark-submit \
    --master spark://10.176.62.230:7077 \
    --driver-memory 2g \
    --executor-memory 4g \
    --executor-cores 2 \
    --num-executors 2 \
    /root/baseline_clustering.py \
    --input hdfs://node5:9000/user/root/data/oracle_database_questions_jsonlines.json \
    --output hdfs://node5:9000/user/root/output/baseline_clustering \
    --k 50 \
    --max-features 5000 \
    --sample-ratio 0.1
```

### 4. 查看结果

```bash
# 查看D实验输出目录
hdfs dfs -ls /user/root/output/d_word2vec_experiment

# 查看D1结果
hdfs dfs -ls /user/root/output/d_word2vec_experiment/d1_tfidf
hdfs dfs -cat /user/root/output/d_word2vec_experiment/d1_tfidf/cluster_assignments/* | head -20

# 查看D4结果
hdfs dfs -ls /user/root/output/d_word2vec_experiment/d4_tfidf_weighted_w2v
hdfs dfs -cat /user/root/output/d_word2vec_experiment/d4_tfidf_weighted_w2v/duplicate_candidates/* | head -20

# 查看旧版基线结果
hdfs dfs -ls /user/root/output/baseline_clustering
hdfs dfs -cat /user/root/output/baseline_clustering/cluster_summary/*
```

### 5. 停止服务

```bash
# 停止Workers
ssh root@10.176.62.231 "/opt/spark-2.0.0-bin-hadoop2.7/sbin/stop-slave.sh"
ssh root@10.176.62.232 "/opt/spark-2.0.0-bin-hadoop2.7/sbin/stop-slave.sh"

# 停止Master
/opt/spark-2.0.0-bin-hadoop2.7/sbin/stop-master.sh

# 停止HDFS
/opt/hadoop/sbin/stop-dfs.sh
```

---

## D实验方案说明

### 实验设计

| 方案 | 语义特征方法 | 说明 |
|------|------------|------|
| **D1** | None (TF-IDF baseline) | C1基线: title+body, vocab=5000, min_df=5, max_df=0.9 |
| **D2** | Word2Vec average | 纯Word2Vec简单平均向量 |
| **D3** | TF-IDF + W2V concatenation | TF-IDF向量与Word2Vec向量拼接 |
| **D4** | TF-IDF weighted W2V | TF-IDF加权Word2Vec向量 |
| **D5** | Title(2.0) + Body(1.0) fusion | 标题向量与正文向量分权融合 |

### 统一参数

| 参数 | 值 | 说明 |
|------|---:|------|
| K | 45 | B成员确定的最优K值 |
| 输入数据 | fixed_eval_sample_10pct_plus_300 | 固定样本(15813条) |
| 标注数据 | dedup_pairs_300_reviewed.csv | 300对标注评测数据 |
| similarity_threshold | 0.55 | 重复候选相似度阈值 |
| dedup_top_per_cluster | 80 | 每簇最大候选对数 |

### 评测指标

| 指标 | 含义 |
|------|------|
| **Candidate Recall** | 真实重复问题对中，被聚到同一簇的比例 |
| **Precision** | 被判为重复的问题对中，真实重复的比例 |
| **Recall** | 真实重复问题对中，被成功识别的比例 |
| **F1** | Precision和Recall的综合指标 |
| **运行时间** | 特征构建+聚类+候选生成的总时间 |

### 输出表格格式

```
| 方案 | 语义特征方法 | Candidate Recall | Precision | Recall | F1 | 运行时间 |
| -- | ------ | ---------------: | --------: | -----: | -: | ---: |
```

---