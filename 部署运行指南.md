## 使用三个节点运行基准方案的完整步骤

> **重要说明：Spark版本配置**
> 
> 当前集群使用Spark 2.0（支持Python 2）：
> - 230 (node5): Spark 2.0.0
> - 231 (node1): Spark 2.0.0（从230复制）
> - 232 (node2): Spark 2.0.0（从230复制）
> 
> **原因：Spark 3.x需要Python 3，但集群无Python 3。Spark 2.0支持Python 2。**

---

### 第一步：上传文件到230节点

在你的本地Windows电脑上，打开PowerShell，执行以下命令上传文件：

```powershell
# 上传代码文件
scp "d:\OneDrive - shoutoutuoadi325\桌面\大数据\PJ\baseline_clustering.py" root@10.176.62.230:/root/

# 上传数据文件
scp "d:\OneDrive - shoutoutuoadi325\桌面\大数据\PJ\StackOverFlow_Oracle_Database\oracle_database_questions.json" root@10.176.62.230:/root/
```

输入密码：`Fdwangp123`

---

### 第二步：在230节点配置环境

回到已连接的node5（230），执行：

```bash
# 设置环境变量（使用Spark 2.0）
export SPARK_HOME=/opt/spark
export PATH=$SPARK_HOME/bin:$SPARK_HOME/sbin:$PATH
export HADOOP_HOME=/opt/hadoop
export PATH=$HADOOP_HOME/bin:$HADOOP_HOME/sbin:$PATH

# 验证Spark版本
$SPARK_HOME/bin/spark-submit --version

# 验证文件
ls -lh baseline_clustering.py oracle_database_questions.json
```

---

### 第三步：上传数据到HDFS

```bash
# 创建HDFS目录
hdfs dfs -mkdir -p /user/root/data

# 上传数据文件
hdfs dfs -put oracle_database_questions.json /user/root/data/

# 验证上传成功
hdfs dfs -ls /user/root/data/
hdfs dfs -du -h /user/root/data/oracle_database_questions.json
```

---

### 第四步：部署Spark 2.0到Worker节点

将230上的Spark 2.0打包并复制到231/232：

```bash
# 在230上打包Spark 2.0（/opt/spark是软链接，指向spark-2.0.0-bin-hadoop2.7）
cd /opt
tar -czf spark-2.0.0-bin-hadoop2.7.tar.gz spark-2.0.0-bin-hadoop2.7

# 复制到231（密码：Fdwangp123）
scp spark-2.0.0-bin-hadoop2.7.tar.gz root@10.176.62.231:/opt/

# 复制到232（密码：Fdwangp123）
scp spark-2.0.0-bin-hadoop2.7.tar.gz root@10.176.62.232:/opt/

# 在231上解压
ssh root@10.176.62.231 "cd /opt && tar -xzf spark-2.0.0-bin-hadoop2.7.tar.gz"

# 在232上解压
ssh root@10.176.62.232 "cd /opt && tar -xzf spark-2.0.0-bin-hadoop2.7.tar.gz"

# 创建软链接（方便后续使用）
ssh root@10.176.62.231 "ln -sf /opt/spark-2.0.0-bin-hadoop2.7 /opt/spark"
ssh root@10.176.62.232 "ln -sf /opt/spark-2.0.0-bin-hadoop2.7 /opt/spark"

# 验证Spark 2.0已安装
ssh root@10.176.62.231 "ls -la /opt/spark-2.0.0-bin-hadoop2.7"
ssh root@10.176.62.232 "ls -la /opt/spark-2.0.0-bin-hadoop2.7"
```

---

### 第五步：启动Spark Master

```bash
# 设置环境变量
export SPARK_HOME=/opt/spark-2.0.0-bin-hadoop2.7

# 启动Master（在230/node5上，使用完整路径）
/opt/spark-2.0.0-bin-hadoop2.7/sbin/start-master.sh --host 10.176.62.230 --port 7077 --webui-port 8080

# 等待Master启动
sleep 3

# 验证Master启动成功
jps | grep Master

# 检查Master Web UI端口（8080）
netstat -tlnp | grep 8080
```

---

### 第六步：启动Spark Workers

```bash
# 在231上启动Worker（设置SPARK_HOME环境变量）
ssh root@10.176.62.231 "export SPARK_HOME=/opt/spark-2.0.0-bin-hadoop2.7 && /opt/spark-2.0.0-bin-hadoop2.7/sbin/start-slave.sh spark://10.176.62.230:7077"

# 在232上启动Worker（设置SPARK_HOME环境变量）
ssh root@10.176.62.232 "export SPARK_HOME=/opt/spark-2.0.0-bin-hadoop2.7 && /opt/spark-2.0.0-bin-hadoop2.7/sbin/start-slave.sh spark://10.176.62.230:7077"

# 等待Worker启动
sleep 5

# 验证Worker进程
ssh root@10.176.62.231 "jps"
ssh root@10.176.62.232 "jps"
```

---

### 第七步：验证集群状态

```bash
# 验证集群启动成功
jps
ssh root@10.176.62.231 "jps"
ssh root@10.176.62.232 "jps"
```

### 第八步：访问Spark Web UI

浏览器访问：`http://10.176.62.230:8080`

应该看到：
- **Spark Master at spark://10.176.62.230:7077**
- **Alive Workers: 2**


```bash
# 使用iptables开放端口
iptables -I INPUT -p tcp --dport 9000 -j ACCEPT
iptables -I INPUT -p tcp --dport 50070 -j ACCEPT
iptables -I INPUT -p tcp --dport 7077 -j ACCEPT
iptables -I INPUT -p tcp --dport 8080 -j ACCEPT
iptables -I INPUT -s 10.176.62.231 -p tcp --dport 9000 -j ACCEPT
iptables -I INPUT -s 10.176.62.232 -p tcp --dport 9000 -j ACCEPT
iptables -I INPUT -s 10.176.62.231 -p tcp --dport 50070 -j ACCEPT
iptables -I INPUT -s 10.176.62.232 -p tcp --dport 50070 -j ACCEPT
iptables -I INPUT -s 10.176.62.231 -p tcp --dport 50010 -j ACCEPT
iptables -I INPUT -s 10.176.62.232 -p tcp --dport 50010 -j ACCEPT

# 直接将当前内存中的规则写入配置文件
iptables-save > /etc/sysconfig/iptables

# 查看配置文件中是否包含你刚添加的规则
cat /etc/sysconfig/iptables | grep -E '9000|50070|7077|8080'

# 在231上检查防火墙
ssh root@10.176.62.231 "iptables -L -n | grep 9000"

# 如果没有，在231上添加规则
ssh root@10.176.62.231 "iptables -I INPUT -p tcp --dport 9000 -j ACCEPT"
ssh root@10.176.62.231 "iptables -I INPUT -p tcp --dport 50070 -j ACCEPT"

# 同样在232上
ssh root@10.176.62.232 "iptables -I INPUT -p tcp --dport 9000 -j ACCEPT"
ssh root@10.176.62.232 "iptables -I INPUT -p tcp --dport 50070 -j ACCEPT"

# 修改231的hosts文件
ssh root@10.176.62.231 "sed -i 's/10.176.62.235 node5/10.176.62.230 node5/' /etc/hosts"

# 修改232的hosts文件
ssh root@10.176.62.232 "sed -i 's/10.176.62.235 node5/10.176.62.230 node5/' /etc/hosts"

# 验证修改成功
ssh root@10.176.62.231 "grep node5 /etc/hosts"
ssh root@10.176.62.232 "grep node5 /etc/hosts"

# 验证node5现在指向正确的IP
ssh root@10.176.62.231 "getent hosts node5"
ssh root@10.176.62.232 "getent hosts node5"
```

---

### 第九步：运行基准方案

```bash
# 使用Spark 2.0运行
export SPARK_HOME=/opt/spark
export HADOOP_HOME=/opt/hadoop

spark-submit \
    --master spark://10.176.62.230:7077 \
    --driver-memory 2g \
    --executor-memory 4g \
    --executor-cores 2 \
    --num-executors 2 \
    baseline_clustering.py \
    --input hdfs://node5:9000/user/root/data/oracle_database_questions_jsonlines.json \
    --output hdfs://node5:9000/user/root/output/baseline_clustering \
    --k 50 \
    --max-features 5000 \
    --sample-ratio 0.1

```

---

### 第十步：查看结果

```bash
# 查看输出目录
hdfs dfs -ls /user/root/output/baseline_clustering

# 查看聚类摘要
hdfs dfs -cat /user/root/output/baseline_clustering/cluster_summary/*

# 查看部分聚类分配结果
hdfs dfs -cat /user/root/output/baseline_clustering/cluster_assignments/* | head -20
```

---

### 第十一步：停止集群（任务完成后）

```bash
# 停止231上的Worker（设置SPARK_HOME环境变量）
ssh root@10.176.62.231 "export SPARK_HOME=/opt/spark-2.0.0-bin-hadoop2.7 && /opt/spark-2.0.0-bin-hadoop2.7/sbin/stop-slave.sh"

# 停止232上的Worker（设置SPARK_HOME环境变量）
ssh root@10.176.62.232 "export SPARK_HOME=/opt/spark-2.0.0-bin-hadoop2.7 && /opt/spark-2.0.0-bin-hadoop2.7/sbin/stop-slave.sh"

# 停止Master（在230上）
export SPARK_HOME=/opt/spark-2.0.0-bin-hadoop2.7
/opt/spark-2.0.0-bin-hadoop2.7/sbin/stop-master.sh

# 验证所有进程已停止
jps
ssh root@10.176.62.231 "jps"
ssh root@10.176.62.232 "jps"
```
---

## 监控Web UI

| UI | 地址 | 说明 |
|----|------|------|
| Spark集群 | http://10.176.62.230:8080 | 查看Worker状态 |
| 任务详情 | http://10.176.62.230:4040 | 查看任务执行进度 |


